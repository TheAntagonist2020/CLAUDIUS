# Required for TMDb enrichment
TMDB_API_KEY=your_tmdb_api_key

# MDBList API (default already set in config)
MDBLIST_API_KEY=pzoitdmb31gwbtrfmgrq951rp

# Railway sets PORT automatically — don't set manually
# PORT=3001

# SQLite database path — Railway uses a persistent volume
# DATABASE_PATH=/data/claudius.db
